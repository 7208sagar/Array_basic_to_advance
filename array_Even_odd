const arr=[2,3,4,5,9,8];
const even =[];
const odd =[];
for(let i =0;i<arr.length;i++){
    if(arr[i] % 2 ===0){
        even.push(arr[i]);
    }
    if(arr[i] % 2 !==0){
        odd.push(arr[i]);
    }
}
console.log("even--",even);
console.log("odd--",odd);
